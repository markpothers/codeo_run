

class Landscape  {
    constructor(name, x, y){
        
        this.name = name;
        this.x = x;
        this.y = y;
        // this.character_x = character_x;
        // this.character_y = character_y;
        // this.id = character_id;

        //the above line -am not sure about it. Just saw the schema and had the one above.
        //So commenting out for now.
        //this.direction = null;
       // this.img = img
       //we are not letting the user choose this. 
       //we are gonna select one of the assets saved here

        // this.element = document.createElement('div');
        // this.image = document.createElement('img');
        // this.element.append(this.image);
        // this.element.append(this.name);
        // this.element.style.position = 'absolete';
        // this.element.style.left = `${characterData.x}px`;
        // this.element.style.bottom = `${characterData.y}px`;

        // this.element.style.zIndex = window.innerWidth - characterData.y;
        // //this.image.src = `${ASSET_ROOT}/---/static.gif`;//save a statuc image in the working tree and use that path here
        // this.image.src = 'assets/desert_BG.png';
        //document.body.append(this.element);
    }
}

